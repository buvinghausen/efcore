# SDD ledger — plan: .superpowers/2026-08-21-temporal-entity-splitting-implementation-plan.md

Spec: `.superpowers/2026-08-14-temporal-entity-splitting-design.md` (rev 3.1) — the binding authority.
Worktree: `/home/buvy/code/buvinghausen/efcore/.claude/worktrees/feature+per-store-object-constraint-names`
Branch: `worktree-feature+per-store-object-constraint-names` (created by EnterWorktree from origin/main @ 2965e616bf)
Baseline: `./build.sh --test --projects <abs>/test/EFCore.Relational.Tests/EFCore.Relational.Tests.csproj` — GREEN, 3m19s, 0 errors.

## Scope of this run

Ruling: this run executes **Phase B only (Tasks B1–B10)** — why: the plan states the two phases are independently shippable, touch disjoint assemblies, and use separate branches; SDD assumes one branch. Phase A gets its own SDD run on `feature/temporal-entity-splitting` branched from main. Cost if wrong: nothing — the phases have no cross-references.

Ruling: the branch is named `worktree-feature+per-store-object-constraint-names` rather than the plan's `feature/per-store-object-constraint-names` — why: EnterWorktree owns branch naming and fighting the harness creates phantom state; the PR branch name matters only at push time, which is a stop-and-ask step anyway (B10 Step 3). Cost if wrong: one `git branch -m` before pushing.

## Pre-flight conflict scan

Task pairs sharing a file or interface:

| Pair | Produces → Consumes | Finding |
|---|---|---|
| B1 → B2 | `RelationalKeyOverrides.Find` | clean. B2 touches `Metadata/Internal/RelationalKeyExtensions.cs`; verified both an Internal and an Extensions file of that name exist, so B2 and B6 touch different files. |
| B1 → B3 | shape/substitution table | clean. |
| B3 → B4 | `RelationalForeignKeyOverrides.Find` | clean, same Internal/public split as B2. |
| B1,B3 → B5 | `Attach`/`MergeInto` on the override types | **CONFLICT (F2)** — B5's convention body calls `key.GetOverrides()` / `key.RemoveOverrides(storeObject)`, which Task B6 produces. Plan flags it and offers two options. |
| B5 → B6 | conventions vs public API | consequence of F2; see ruling. |
| B6 → B8 | fluent API the snapshot emits | clean — B8 emits `HasName(name, storeObject)` from B6. |
| B6 → B9 | `SetConstraintName(name, dependent, principal)` | clean — B9 runs after B6. |
| B1,B3 → B7 | override types → runtime types | clean. |
| all → B10 | baseline + sweep | **CONFLICT (F3)** — see ruling. |
| B2/B4 ↔ B9 | `GetName`/`GetConstraintName` resolution vs new validator | clean — B9 reads the resolvers, does not change them. |

Task self-consistency:

| Task | Finding |
|---|---|
| B1 | **CONFLICT (F1)** — Files list omits `test/EFCore.Relational.Tests/RelationalApiConsistencyTest.cs`; Step 7 says "Expected: PASS". Verified: that fixture registers metadata quartets in `_metadataTypes` (`:87`) and `RelationalMetadataTypes` (`:122`), including `IReadOnlyRelationalPropertyOverrides`. A new quartet not registered there fails the suite. |
| B2 | clean — verified public `GetName(this IReadOnlyKey, in StoreObjectIdentifier)` exists (`Extensions/RelationalKeyExtensions.cs:36`), and `TestHelpers.CreateConventionBuilder` exists. |
| B3 | **CONFLICT (F1)** — same omission for the FK quartet. |
| B4 | clean. |
| B5 | test/impl agreement fixed in review round 1 (forces real key replacement); Step 4b audit added. Clean as written. |
| B6 | Files list names `RelationalKeyBuilderExtensions.cs` twice (an artifact of the round-1 assembly-boundary fix). Benign duplication, no action. |
| B7 | clean — round-1 fix points the run at the provider suites that discover the fact. |
| B8 | clean — verified `CSharpMigrationsGeneratorTest` is a **concrete** `partial class` deriving `CSharpMigrationsGeneratorTestBase` (`:18`), so B7's abstract-base discovery problem does not recur here. |
| B9 | clean — verified `test/EFCore.Relational.Tests/Migrations/Internal/MigrationsModelDifferTest.cs` exists and validator call site `:1029` is real. |
| B10 | **CONFLICT (F3)** — Step 1 points `RelationalApiConsistencyTest` at `baseline.json`, but baseline.json is consumed by a *different* project, `test/EFCore.ApiBaseline.Tests/ApiBaselineTest.cs:24`. Step 1's expected-entry list also still names `IConventionKeyBuilder`/`IConventionForeignKeyBuilder` members, which review round 1 moved to extension methods. Step 3 pushes to origin and opens an upstream PR. |

## Pre-flight rulings

Ruling: **F1** — B1 and B3 each also modify `test/EFCore.Relational.Tests/RelationalApiConsistencyTest.cs`, registering the new quartet in `_metadataTypes` (with `null!` in the ConventionBuilder slot, matching the `IReadOnlyRelationalPropertyOverrides` entry, since the overrides builder is internal) and adding the runtime interface to `RelationalMetadataTypes`. Why: without it the task's own "Expected: PASS" is unreachable, and the plan's precedent entry proves the convention. Cost if wrong: one extra test file touched that upstream would have required anyway.

Ruling: **F2** — B5's conventions call the Task B1/B3 statics directly (`RelationalKeyOverrides.Get(key)` / `.Remove(key, storeObject)`, and the FK equivalents) instead of introducing extension methods; B6 later adds the public `GetOverrides`/`RemoveOverrides` extensions delegating to those statics. Why: B1 already produces `Get` and `Remove`, so no new surface is needed, and adding internal extensions in `Metadata.Internal` alongside identically-named public ones in `Microsoft.EntityFrameworkCore` would be ambiguous inside the Relational assembly. Cost if wrong: B6 refactors two convention bodies to the extension form — trivial.

Ruling: **F3** — B10 Step 1 splits in two: `RelationalApiConsistencyTest` (in `EFCore.Relational.Tests`) for the consistency checks, and `test/EFCore.ApiBaseline.Tests` for `EFCore.Relational.baseline.json`; the expected-entry list drops the two builder-interface rows. `EFCore.ApiBaseline.Tests` joins the Step 2 sweep. Why: verified `ApiBaselineTest.cs:24` is the only reader of baseline.json, and round 1 moved the convention seam off the interfaces. Cost if wrong: a wasted test run.

Ruling: **B10 Step 3 (push + upstream PR) is a stop-and-ask.** Why: pushing a shared branch and opening a PR on dotnet/efcore are outward-facing side effects. Execution stops after B10 Step 2 and surfaces the PR step to the user. Cost if wrong: none — the work is committed either way.

## Progress

Task B1: dispatched (implementer, sonnet) — BASE 2965e616bfbeae78ee40207ac08d513474e995b6. Carried rulings F1 (register the quartet in `RelationalApiConsistencyTest`) and F2 (no `GetOverrides`/`RemoveOverrides` extensions; later tasks use the `Get`/`Remove` statics).
Task B1: implementer DONE — commit 9e42b97b55, 10 files, +723/-1. Reported 1484 passed / 0 failed, 0 warnings.
Task B1: task review dispatched (sonnet) against 2965e616bf..9e42b97b55.
Task B1: review clean — spec ✅, quality Approved, zero findings at every severity. Reviewer independently cross-checked the .trx (1484 passed / 0 failed) and diffed each new interface against its precedent.
Task B1: complete (commits 2965e616b..9e42b97b5, review clean)
Task B2: dispatched (implementer, sonnet) — BASE 9e42b97b5580842f092a635d4ab058ef9b23718e. Carried ruling F4 (test-snippet substitutions) plus the note that B1's test file and its `Customer` fixture already exist and are to be reused.
Task B2: implementer DONE_WITH_CONCERNS — commit 3ce4f73f94, 2 files, +94/-2. Reported 1488 run / 1 skipped / 0 failed.

Ruling: **F5 — the plan's `Customer` test fixture cannot survive `FinalizeModel()` once split.** The implementer added an `Address` property to the shared fixture; accepted. Why: the plan's two-property `Customer` (`Id`, `Name`) with `SplitToTable("CustomerDetails", s => s.Property(c => c.Name))` leaves the principal table holding only the primary key, which relational model validation rejects — the plan's snippet was written without exercising `FinalizeModel()`, which B2's third test is the first to call. One added property is the minimal fix and the implementer cites an existing repo precedent. The task review is judging the change independently rather than taking the rationale on trust. Cost if wrong: a one-property divergence from the plan text, visible in the diff. Note for later dispatches: B5 and B9 reuse this same fixture with `SplitToTable`, so they inherit the fix.
Task B2: task review dispatched (sonnet) against 9e42b97b55..3ce4f73f94.
Task B2: review clean — spec ✅, quality Approved. Reviewer independently confirmed both replacement sites, byte-for-byte preservation of `GetName`'s control flow, that `IsNameOverridden` really distinguishes explicit-null from unset (`RelationalKeyOverrides.cs:204`), that the validator error driving F5 is genuine (`EntitySplittingUnmappedMainFragment`), and that B1's three tests neither use `SplitToTable`/`FinalizeModel()` nor reference the added property.
Task B2: minor (deferred): the report summarised test counts in prose rather than pasting the raw runner Passed/Failed/Skipped/Total line, making verification arithmetic rather than mechanical. Resolved forward, not retroactively — later dispatches instruct implementers to paste the runner summary verbatim.
Task B2: ⚠️ resolved by controller — the reviewer could not mechanically verify the reported counts. Not a gap: the arithmetic is self-consistent (1484 baseline passed + 1 pre-existing skip + 3 new = 1488 run) and the suite is green either way. No fix round.
Task B2: complete (commits 9e42b97b5..3ce4f73f9, review clean)
Task B3: dispatched (implementer, sonnet) — BASE 3ce4f73f94a325ae585382b214e73e28636ff74b.
Task B3: implementer DONE — commit 297073f8ed. Runner line: total=1491 executed=1490 passed=1490 failed=0 notExecuted=1.

Ruling: **F6 — the linking foreign key does not exist before `FinalizeModel()`, and does not survive it in a mutable form.** The brief's `Foreign_key_overrides_are_keyed_by_the_dependent_principal_pair` test cannot run as written: `EntitySplittingConvention.ProcessModelFinalizing` (`src/EFCore.Relational/Metadata/Conventions/EntitySplittingConvention.cs:80`) is what creates the self-referential linking FK, at `:99` via `entityType.Builder.HasRelationship(entityType, pk.Properties, pk)` — and finalization makes the model read-only, so `GetOrCreate` would throw on the FK it just produced. Accepted the implementer's fix: construct the identical relationship with that same call before finalizing. Verified `:99` matches the call it replicates. Cost if wrong: the test exercises a hand-built linking FK rather than a convention-built one; the shape is identical, and B9's end-to-end test is where a convention-built one gets covered.
**Carry-forward:** B5's FK survival test and B9's `Rewriting_constraint_names_per_store_object_produces_no_collisions` both reach for `entityType.GetForeignKeys().Single(fk =&gt; fk.PrincipalEntityType == entityType)` on a non-finalized model and hit the same wall. Both dispatches must carry F6.
Task B3: task review dispatched (sonnet) against 3ce4f73f94..297073f8ed. Named risks handed to the reviewer: fidelity to B1's key family, `StoreObjectPair` equality/hashing as a dictionary key, and whether the deviating test still proves independent resolution across pairs.
Task B3: review clean — spec ✅, quality Approved. Reviewer confirmed `StoreObjectIdentifier` has field-wise `IEquatable` with a combined hash (`:205`), so the `record struct` pair's generated equality is correctly order-sensitive; confirmed the builder-only precedence gate; confirmed the `GetValues()` `.ThenBy(PrincipalStoreObject.Name)` ordering; and judged the F6 deviation still proves the composite-identity claim (one FK object, two pair-keyed names, sharing a principal and differing in dependent — the realistic Spike 2 shape).
Task B3: minor (deferred): `IConventionRelationalForeignKeyOverrides.cs:20` and `IConventionRelationalKeyOverrides.cs:20` both read "Gets the builder that can be used to configure this **function**" on public XML doc text.

Ruling: **F7 — the "function" doc wart is an upstream bug, faithfully mirrored.** Verified it is present in untouched upstream code at `src/EFCore.Relational/Metadata/IConventionRelationalPropertyOverrides.cs:20`, which both new files copied. Fix it on the two files this branch owns during B10's pre-PR polish, and note the upstream occurrence in the PR body rather than editing an otherwise-untouched upstream file — an upstream PR is easier to review when its diff contains only its subject. Cost if wrong: a reviewer asks for the third one too; one line.
Task B3: complete (commits 3ce4f73f9..297073f8e, review clean)
Task B4: dispatched (implementer, sonnet) — BASE 297073f8ed8b66f2735d1b0b30afde50afc76341. Carried F4, F6, and the no-new-public-surface constraint.
Task B4: implementer ended its turn mid-task while waiting on a backgrounded test run — no report, nothing committed, only the test file modified (end of the RED step). Resumed it with the current worktree state and an instruction to run the suite in the foreground. Not a fix round: the task never produced a first report.
Task B4: implementer DONE_WITH_CONCERNS — commit 73dc2c7381. Runner line: total=1494 executed=1493 passed=1493 failed=0 notExecuted=1. Reports one global-name fallback site in `GetConstraintName`, covered. Split the brief's one test into three and raised an architectural concern about the entity-splitting linking FK.

Ruling: **F8 — the linking-FK finding is real, but narrower than the report frames it, and the workstream's premise survives.** Everything the implementer root-caused is confirmed by my own reading: `ModelCleanupConvention.RemoveNavigationlessForeignKeys` (`src/EFCore/Metadata/Conventions/ModelCleanupConvention.cs:80`) deletes any declared FK with no navigations and no referencing skip navigations, which is exactly the shape of a linking FK; it is registered by `ProviderConventionSetBuilder.CreateConventionSet()` (`:55`), which `RelationalConventionSetBuilder` calls at `:57` **before** adding `EntitySplittingConvention` at `:73`; and `EntitySplittingConvention.ProcessModelFinalizing` (`:99`) then creates a fresh FK carrying no annotations. So a hand-built stand-in FK cannot survive to receive an override, and that is not fixable in B4.

What the report left open — and what decides whether Phase B delivers its driving use case — is whether *anything* can attach an override to the real linking FK. It can, and the path is unambiguous: `ConventionSet.ModelFinalizingConventions` is a `List<>` whose `Add` appends (`ConventionSet.cs:708`), `ConventionDispatcher.ImmediateConventionScope` iterates it in registration order (`:43`), and `RuntimeConventionSetBuilder.CreateConventionSet()` applies every `IConventionSetPlugin.ModifyConventions` **after** the provider builder has run (`:38-45`). A plugin's finalizing convention — which is precisely what EFCore.NamingConventions is — therefore runs after `EntitySplittingConvention`, sees the linking FK it just created, and finds the model still mutable, since `FinalizeModel()` only nulls the dispatcher once finalizing conventions have finished. The gap is "no override before finalization", not "no override ever". Cost if wrong: five tasks of work resting on a wrong premise — which is why B9 is now the empirical proof rather than a formality.

**Carry-forward, replacing part of F6:** B9's `Rewriting_constraint_names_per_store_object_produces_no_collisions` cannot set overrides on a pre-finalization model at all. It must be rewritten to apply them from an `IModelFinalizingConvention` appended after `EntitySplittingConvention` — which is a *more* faithful reproduction of the NamingConventions #396 integration than the plan's version, and is the test that confirms the paragraph above.
Task B4: review clean — spec ✅, quality Approved. Reviewer independently confirmed exactly one global-name fallback site by reading the whole method, identified `RelationalModel.PopulateForeignKeyConstraints:2095` as the single call site the replacement test exercises, confirmed `EntitySplittingConvention.cs` is byte-identical after the temporary instrumentation, and judged the pre-passing boundary test to be guarding the `defaultName != null` gate rather than asserting nothing.
Task B4: complete (commits 297073f8e..73dc2c738, review clean)
Ruling: **F9 — the 3.5-minute test cycle was self-inflicted; switch every remaining task to a warm-node inner loop.** `build.sh` forwards `--nodeReuse false` to Arcade, so every invocation discards the warm MSBuild nodes and re-evaluates the whole repo. Measured in this worktree: `./build.sh --test --projects <abs>` 3m19s; a plain `.dotnet/dotnet build test/EFCore.Relational.Tests/EFCore.Relational.Tests.csproj -v q --nologo` 2m11s cold but **2.07s** on the next invocation; running the built dll directly with `--filter-class '*RelationalKeyOverridesTest'` 971ms for 7 tests. Inner loop goes from ~3.5 min to ~3s. Every dispatch from B6 on gets this recipe, with one full `./build.sh --test` before committing to catch cross-test regressions. Cost if wrong: a stale incremental build hides a failure the pre-commit full run then catches anyway.

Task B5: implementer DONE_WITH_CONCERNS — commit a6e1d62b6f. Runner: total=1496 executed=1495 passed=1495 failed=0 notExecuted=1. Audit came back with **every** attach chain delayed, no counterexample — the convention design stands and the `InternalKeyBuilder.Attach` fallback was not needed.

Ruling: **F10 — added-conventions alone are insufficient, and this partly vindicates a review-round-1 objection I rejected.** The implementer found, via a failing test rather than speculation, that `InternalKeyBuilder.Attach` sometimes **reuses** an existing key rather than creating one — `HasKeyInternal:267` finds it via `FindDeclaredKey` and returns it at `:325`. On that path `MergeAnnotationsFrom` still copies the stale override across, but no added-event fires, so the convention never runs and the back-pointer stays stale. It supplemented the design with `IKeyAnnotationChangedConvention`/`IForeignKeyAnnotationChangedConvention` filtered to the overrides annotation, entirely within Relational.

Recording this accurately: in review round 1 the external reviewer proposed reworking B5 "around annotation-changed processing", and I pushed back, arguing the added-convention mechanism works because delayed conventions replay after `MergeAnnotationsFrom`. That argument was correct as far as it went — the audit confirms all chains are delayed — but it answered only the *re-created* path. Neither of us identified the *reused* path, where no added-event exists to replay at all. The reviewer's suggested mechanism turns out to be needed as a supplement, not a replacement. Cost if wrong: two conventions where one would do; the task review is judging whether the added-convention is now redundant.
Task B5: task review dispatched (sonnet) against 73dc2c7381..a6e1d62b6f. Asked it specifically whether the two conventions overlap, whether the annotation filtering handles removals, and whether the pair can recurse.
Task B5: review **Needs fixes** — two Important findings, first of the phase. (1) The Added-conventions are redundant: `InternalKeyBuilder.Attach:44` merges annotations unconditionally, `AnnotatableBuilder.MergeAnnotationsFrom:184` re-sets every annotation, and `Key.OnAnnotationSet:137` fires annotation-changed on each — so AnnotationChanged covers the re-created path too, and Added is never independently useful. (2) The Step 4b audit omitted the `ComplexPropertySnapshot.cs:172` row the brief names by hand; the reviewer traced it independently (both call sites delayed, at `InternalComplexTypeBuilder.cs:253` and `InternalEntityTypeBuilder.cs:1397`) so the conclusion holds, but the deliverable did not enumerate what it was asked to. Reviewer also confirmed no recursion risk — `Remove`/`GetOrCreate` mutate the `StoreObjectDictionary` in place rather than reassigning the annotation, so the convention cannot re-trigger itself.
Task B5: fix round 1/5 dispatched — resumed the original implementer with both findings verbatim, plus an instruction to keep both interfaces and say so if it finds a real path where a stale-override-carrying key is added without `MergeAnnotationsFrom` running. Also handed it the F9 fast test loop.
Ruling: **F11 — B6's tests land in a library that never runs, and there is no SQL Server here.** `test/EFCore.Relational.Specification.Tests` sets `IsTestProject=false` and builds no executable host, so `RelationalModelBuilderTest` only executes through provider subclasses (`SqliteModelBuilderTestBase:8`, `SqlServerModelBuilderTestBase`). This is the same discovery trap review round 1 fixed in B7, which the plan never applied to B6. `Test__SqlServer__DefaultConnection` is unset in this environment, so **Sqlite is the runner** for B6's new test; the SQL Server subclass inherits it and is covered when a database is available (B10's sweep, which is a stop-and-ask boundary anyway). Cost if wrong: the SQL Server-specific half of the model-builder coverage goes unrun until someone has a database.

Task B5: fix round 1/5 (2 addressed, 0 open — Added interfaces dropped from both conventions and XML docs rewritten; missing `ComplexPropertySnapshot.cs:172` audit row added; commits a6e1d62b6..1333fae5c). Implementer found no counterexample path and reports identical counters to pre-fix (total=1496 executed=1495 passed=1495 failed=0 notExecuted=1), which is the evidence the simplification is behavior-preserving. Scoped re-review dispatched against a6e1d62b6f..1333fae5c9.
Task B5: re-review — both findings ADDRESSED, no new breakage, no out-of-scope observations. Re-reviewer independently re-traced the removal's safety through `AnnotatableBase.SetAnnotation:122`/`AddAnnotation:63` → `Key.OnAnnotationSet:133`, confirmed `InternalForeignKeyBuilder.Attach:3467` routes every non-null branch through `ReplaceForeignKey`, checked no baseline or dispatch-list test referenced the removed interfaces, and ran both focused test classes itself (7/7 each).
Task B5: complete (commits 73dc2c738..1333fae5c, review clean after 1 fix round)
Ruling: **F12 — B5's attach audit was incomplete in all three passes; corrected with LSP call-graph queries, conclusion unchanged.** A C# language server became available mid-run. `findReferences` on `PropertiesSnapshot.Attach` returned 7 sites including `EntityType.cs:4670`, which my grep-derived list never had; `findReferences` on the enclosing `EntityType.Snapshot.Attach` (`:4626`) returned two more callers absent from every list — `RelationshipSnapshot.cs:80` and `CollectionCollectionBuilder.cs:450`. Checked the genuinely new one: `CollectionCollectionBuilder.cs:450` sits under `using var _ = LeftEntityType.Model.DelayConventions();` at `:406`, so it is delayed. **Every chain is still delayed and B5's code needs no change** — but the audit table it, the reviewer, and I all signed off on was built from an incomplete enumeration.

Root cause worth remembering: `grep "\.Attach("` cannot distinguish `PropertiesSnapshot.Attach` from four other `Attach` methods, so I filtered by receiver name and dropped rows without noticing. Not reopening B5's fix loop — the defect is in the record, not the code — but the final whole-branch review gets this correction, and B9 plus the final review use LSP call-graph queries rather than grep for anything reference-shaped. Cost if wrong: none identified; the check tightened an existing conclusion rather than overturning it.

Task B6: implementer DONE — commit 4e66f8c570, 8 files, +517/-6. `EFCore.Relational.Tests` total=1496 executed=1495 passed=1495 failed=0 notExecuted=1 (identical to baseline), ApiConsistency 20/20; `EFCore.Sqlite.FunctionalTests` total=38226 executed=37940 passed=37940 failed=0 notExecuted=286, with `SqliteModelBuilderGenericTest+SqliteGenericNonRelationship.Can_configure_per_table_key_and_foreign_key_constraint_names` named and `outcome="Passed"` — which is the evidence F11 demanded, that the test actually executes rather than being silently undiscovered.
Task B6: two things handed to the reviewer to judge rather than accept. (1) The implementer added public API **beyond** the brief's Produces list — `IMutable`/`IConvention`/`I`-level `GetOverrides` mirrors and an `IConvention`-level `RemoveOverrides` — claiming `RelationalApiConsistencyTest` forced them and that `RelationalPropertyExtensions` carries the same family. Extra public surface on a branch headed upstream is exactly what a spec review is for, so the reviewer verifies the claim rather than taking it. (2) An interim run produced 18 spurious `SQLite Error 10: disk I/O error` failures from a leftover background process out of the stalled turn; reviewer confirms the reported numbers come from the clean re-run.
Task B6: task review dispatched (sonnet) against 1333fae5c9..4e66f8c570. Told it to prefer LSP `findReferences` over grep for reference-shaped questions, per F12.
Task B6: review **Needs fixes** — three Important findings. Reviewer verified the two hardest constraints are correct (assembly boundary; precedence split matching the pre-existing global `SetName`/`HasName` pair), read the TRX files off disk to confirm the counters and that "disk I/O error" appears zero times in the clean run, and traced `ApiConsistencyTestBase.cs:371-461` to check the test-forcing claim rather than accept it.

Ruling: **F13 — drop the dead `fromDataAnnotation` parameter.** `RemoveOverrides(IConventionKey, in StoreObjectIdentifier, bool fromDataAnnotation = false)` accepts and documents the parameter but never reads it, removing unconditionally regardless of configuration source. The reviewer verified `MatchConvention` (`ApiConsistencyTestBase.cs:463-510`) matches on name and return type only, never parameters, so the test does not force it; and the precedent it claims to mirror, `RelationalPropertyExtensions.RemoveOverrides` (`:1829`), has no such parameter. A caller passing `fromDataAnnotation: true` gets an unconditional removal — silently different from every other `fromDataAnnotation` in the same diff. Dropping it is strictly better than implementing the guard: it matches the precedent exactly and removes surface from a branch headed upstream. Cost if wrong: if a later task needs source-aware removal, it adds the parameter *with* the guard.

Ruling: **F14 — keep the `IKey`/`IForeignKey`-level `GetOverrides` mirrors; correct the report's claim about them.** The reviewer is right that these two are not test-forced — `RuntimeExtensions` is registered `null!` for `IReadOnlyKey`/`IReadOnlyForeignKey` (`RelationalApiConsistencyTest.cs:238-254`), same as `IReadOnlyProperty`, so nothing scans for them. But `RelationalPropertyExtensions` carries the same not-forced pair, and shipping a key/FK family that is asymmetric with the property family invites exactly the upstream comment we would be avoiding. Keep the methods; the defect is the report asserting the whole set was forced. Cost if wrong: two extra public methods that mirror an existing precedent.

Ruling: **F15 — the plan's single test covers half the surface; expand it.** `Can_configure_per_table_key_and_foreign_key_constraint_names` is verbatim from the brief and asserts only on the key side, despite the FK side being roughly half of the new API and no test anywhere exercising `HasConstraintName(storeObject, principalStoreObject)`. Nothing exercises the `CanSetName`/`CanSetConstraintName` refusal path either — which is the precedence-preservation behaviour this task exists to deliver, and the reason F13's dead parameter went unnoticed. Plan-mandated, and I am overruling the plan: fix it. Cost if wrong: a slightly larger test than the plan drew.

Task B6: minor (deferred): store-object `HasConstraintName` overloads were not added for `OwnershipBuilder`/`OwnershipBuilder<,>`, though the global family has them (`RelationalForeignKeyBuilderExtensions.cs:209,231`). The brief's literal Produces list names only `ReferenceCollectionBuilder`/`ReferenceReferenceBuilder`, so this is a defensible reading of "mirror that method's full overload set exactly". Left as an asymmetry to note in the PR body rather than more public surface; the final review can overturn it.
Task B6: fix round 1/5 dispatched — F13 and F15 to fix, F14 a report correction.
Task B6: fix round 1/5 (3 addressed per implementer, 0 open — dead parameter dropped from both key and FK versions; FK-side and refusal-path coverage added; report corrected. Commits 4e66f8c57..20f4e42f6). Sqlite full suite total=38227 executed=37941 passed=37941 failed=0 notExecuted=286.
Task B6: new concern raised during the fix — the implementer reports `TestReferenceCollectionBuilder`'s Generic/NonGeneric implementations do not implement `IInfrastructure<ReferenceCollectionBuilder<,>>`, making the **pre-existing** single-arg `HasConstraintName(string)` test wrapper dead code, and bridged around it via `GetInfrastructure()`. Handed to the re-reviewer to verify rather than accept: a new FK test that passes by bypassing the API it claims to cover would be worse than the gap it replaced. If the dead-code claim holds it is an upstream finding worth the PR body.
Task B6: re-review — all three findings ADDRESSED, no new breakage. Re-reviewer verified in source that `GenericTestReferenceCollectionBuilder` (`ModelBuilderTest.Generic.cs:1496`) and its NonGeneric twin (`:1630`) implement neither `IInfrastructure<ReferenceCollectionBuilder<,>>` nor the non-generic form, so the pre-existing `HasConstraintName(string)` wrapper (`RelationalTestModelBuilderExtensions.cs:1586-1603`) really is dead code; confirmed `GetInfrastructure()` bridging is already used at `:8-21` and returns the live `ModelBuilder`; and confirmed the new tests call the real public overloads, not a shim. Arithmetic reconciled: one test extended in place, one added, hence +1.
Task B6: complete (commits 1333fae5c..20f4e42f6, review clean after 1 fix round)
Task B6: minor (deferred): the pre-existing `HasConstraintName(string)` test wrapper is dead code upstream — worth a line in the PR body as a drive-by finding, not a fix on this branch.
Task B7+B8: implementer DONE for both — commits e9d0b86f4a (B7) and 322d7e2d97 (B8), 20 files, +1753/-1 (most of it generated compiled-model baselines). `EFCore.Relational.Tests` unchanged at baseline; new tests named and passing in both `CompiledModelSqliteTest` and `CSharpMigrationsGeneratorTest`.
Task B7+B8: four self-declared deviations handed to the reviewer to judge, not accept — (a) `GenerateKeyOverrides` chaining rather than per-statement termination, (b) `GenerateForeignKeyOverrides` dropping the builder-name prefix, (c) runtime constructors taking `RuntimeKey`/`RuntimeForeignKey` instead of the briefs' `IKey`/`IForeignKey`, (d) **no explicit-null test for B7**. (d) is the one that matters: the runtime type must carry `IsNameOverridden` alongside `Name`, and if it drops or defaults that flag an explicit-null override silently degrades to "unset" in every compiled model. "Verified by reading the code" is exactly the claim a test should replace.
Task B7+B8: also claims 4 Sqlite failures (missing `mod_spatialite.so`) and 5 Design failures (Windows-style path validation) are pre-existing. Asked the reviewer to sanity-check that framing — a pre-existing failure is the easiest place to hide a real one.

Note: LSP **diagnostics** on this repo are unreliable — it reported phantom `System.Object is not defined` / missing-namespace errors on `CSharpMigrationsGeneratorTest.ModelSnapshot.cs` immediately after B8 touched it; a direct `dotnet build` of `EFCore.Design.Tests` came back 0 errors 0 warnings. Navigation (`findReferences`) has been reliable; ignore its diagnostics.

Task B7+B8: task review dispatched (sonnet) against 20f4e42f60..322d7e2d97.
Task B7+B8: review — **B7 Approved, B8 Needs fixes.** Two Important findings, and the reviewer cleared three of the four deviations on evidence: (c) is genuinely forced (`RuntimeRelationalPropertyOverrides.cs:21` really does take `RuntimeProperty`, so the briefs' `IKey`/`IForeignKey` was shorthand), (b) is safe (traced `GenerateForeignKey`: `foreignKeyBuilderName` is always already-open chain text for FKs, never a local declaration), and (a) correctly fixed the brief's literal bug — but only for the common case. It also confirmed `IsNameOverridden` is faithfully carried in both runtime types.

Ruling: **F16 — B8's key-override emitter breaks compilation in a real combination, and the reviewer proved it.** When a key carries both per-store-object overrides and a type-qualified fluent annotation (e.g. SQL Server `IsClustered`, `SqlServerKeyBuilderExtensions.cs:30`), `AppendChainedBuilderHeader` emits `var key = ` and leaves the `.HasKey(...)` chain unterminated pending `GenerateAnnotations`; `GenerateKeyOverrides` then splices a bare `key` identifier into the middle of that open expression. The reviewer built a minimal repro and got `CS1002: ; expected` at exactly the predicted junction. No test in the diff or the repo exercises key overrides together with a type-qualified key annotation, which is why it shipped. Fix required. Cost if wrong: migrations snapshots fail to compile for any SQL Server model combining a clustered-key annotation with per-table key names — a plausible real configuration.

Ruling: **F17 — B7's explicit-null gap is load-bearing; overruling the implementer's "verified by reading the code".** The reviewer sharpened the point I raised: B7 introduces **two** independent conversion paths — the in-memory `RelationalRuntimeModelConvention.Create` (`:477`, `:536`) and the NativeAOT codegen `Create` helpers (`RelationalCSharpRuntimeAnnotationCodeGenerator.cs:276-306, 355-389`) — and neither has a test pinning `IsNameOverridden: true, Name: null`. B8's explicit-null test exercises snapshot generation, a disjoint path, so it provides zero coverage here. Reading the constructor is not evidence about either `Create` helper. Cost if wrong: a future refactor collapses explicit-null to "unset" in every compiled model with nothing to catch it.

Task B7+B8: ⚠️ open for the controller — the report evidenced the full Sqlite project run as a bare "Tests succeeded" rather than the counter breakdown, and the class-scoped run showed 4 `mod_spatialite` failures the project run did not. Resolving through the fix round, which re-runs and must report real counters.
Task B7+B8: fix round 1/5 dispatched — F16 (B8 compile bug) and F17 (B7 explicit-null coverage), plus the counter evidence.
Task B7+B8: fix round 1/5 (2 addressed, 0 open — commits 957d85edf9 (F16, key-override chaining vs a type-qualified annotation) and e3111b76ff (F17, explicit-null on both runtime paths)). Implementer reported DONE_WITH_CONCERNS, flagging that `build.sh`'s Sqlite result looked stale.

Ruling: **F18 — `build.sh --test` produced STALE Sqlite evidence this round; treat direct-dll runs as the primary gate.** I verified this myself rather than taking either account. The Sqlite TRX is freshly timestamped (20:59) yet reports exactly the pre-fix `total="38227"`, and `grep -c "survive_the_compiled_model"` against it returns **0** — the four new B7 tests are absent entirely, so that run executed a stale assembly and never picked up the changed `EFCore.Relational.Specification.Tests` library it depends on. The "build.sh green" line in the report is therefore worthless for this round. `EFCore.Relational.Tests` by contrast moved 1496→1498 correctly, so the staleness is specific, not universal — and B6's earlier Sqlite run did move 38226→38227 with its new test named, so that evidence stands.

I then verified the four tests directly: fresh `dotnet build` of the Sqlite project (3m00s), then `--filter-method '*_the_compiled_model*'` → **total: 4, failed: 0, succeeded: 4**. Note the first filter I tried (`*survive_the_compiled_model*`) matched only 2, because two of the four methods are named "survives" — a filter that silently under-matches looks identical to a passing run, which is its own lesson.

Likely cause, and it is partly mine: F9 introduced plain `dotnet build` (node reuse on) alongside `build.sh` (`--nodeReuse false`), and mixing the two drivers can leave inconsistent incremental state. The speedup was real but it bought a sharper evidence hazard. **Standing rule for the rest of the run: named-test direct-dll output is the primary evidence; `build.sh` green is corroboration, never proof, and a count that fails to move when tests were added is a red flag, not a pass.**

Task B7+B8: scoped re-review dispatched against 322d7e2d97..e3111b76ff.
Task B7+B8: re-review — both findings ADDRESSED, no new breakage. Verified independently: `HasFillFactor` (`SqlServerKeyBuilderExtensions.cs:111`) really does land in `typeQualifiedCalls` because its assembly differs from `RelationalModelBuilderExtensions`' (`:2209-2215`), so the new test exercises the previously-broken branch rather than re-covering the working one; `GenerateAnnotations` unconditionally closes the statement before `GenerateKeyOverrides` runs; `forceLocal` is scoped to `GenerateKey`'s call site only, leaving property/index generation untouched; and the emitted baseline literal is the right shape — `new RuntimeRelationalKeyOverrides(key, StoreObjectIdentifier.Table("CustomerDetails", null), true, null)` beside a separate `key.AddAnnotation("Relational:Name", "pk_global")`, which is explicit-null distinct from a global name. The implementer also caught and fixed a covariance trap in its own new tests by asserting `Assert.IsType<RuntimeRelationalKeyOverrides>` rather than resolution equality.
Task B7+B8: complete (commits 20f4e42f6..e3111b76f, review clean after 1 fix round)
Task B9: implementer DONE — commit e0377c3846. `EFCore.Relational.Tests` total=1501 executed=1500 passed=1500 failed=0 notExecuted=1, up from 1498/1497 — the count moved by exactly the 3 tests added, which is the check F18 now requires.

**F8 CONFIRMED EMPIRICALLY.** The finalizing-convention path to the linking FK works. A convention appended to `ConventionSet.ModelFinalizingConventions` after `EntitySplittingConvention` observes the linking FK the instant it is created and attaches per-store-object names to it; `Rewriting_constraint_names_per_store_object_produces_no_collisions` asserts zero duplicate constraint names in the finalized `RelationalModel`. This was the claim five tasks of work rested on, held only by my reading of the convention-ordering code — it is now proven by a passing test rather than by argument. The workstream delivers its driving use case.

Task B9: three deviations handed to the reviewer to judge. (a) The brief's literal validator body false-flagged legitimate `SharedTableConvention` auto-uniquification; fixed with an `AreCompatible` gate — reviewer must confirm the gate narrows rather than guts the check, since a validation that can no longer fire is worse than one that fires too often. (b) The brief's `logger` parameter double-logged; dropped. (c) The conflict test needed explicit `HasColumnName` calls to collapse two FKs into one physical constraint — reviewer judges whether that is still a realistic scenario or a contrivance engineered to pass. Both (a) and (b) surfaced only from a **full** suite run, not a filtered one, which is a point in the implementer's favour.
Task B9: verified `src/EFCore.Relational` builds 0 warnings / 0 errors myself after the hand-edited `RelationalStrings.Designer.cs` — LSP again reported phantom errors on files that build cleanly.
Task B9: task review dispatched (sonnet) against e3111b76ff..e0377c3846.
Task B9: review clean — spec ✅, Approved, zero Critical or Important. The reviewer independently traced the convention-ordering chain (`TestHelpers.cs:212-224` calls `CreateConventionSet()` before `configureConventions`, `ConventionSet.cs:699-708` appends, `ConventionDispatcher.ImmediateConventionScope.cs:40-43` dispatches in order) rather than accepting the claim, and reasoned out why the end-to-end test is a genuine regression guard and not tautological: the same loop calls `SetName` three times with three different store objects, so a regression to a single global annotation would make all three PKs resolve to the last name and fail the distinctness assertion. All three deviations verified correct — the `AreCompatible` gate repairs a real false positive against the pre-existing `Passes_for_incompatible_foreignKeys_within_hierarchy` test, the dropped logger loses no diagnostic because `ValidateSharedForeignKeysCompatibility` already logs over the identical FK set, and the forced `HasColumnName` is what co-locating columns actually means in practice.
Task B9: minor (deferred): `ValidateSharedForeignKeyNameOverrides` keeps only the first FK per structural bucket, so with 3+ entity types sharing a table a mutually-conflicting pair that is each incompatible with the first-seen FK would not be caught. Narrow, bounded by `SharedTableConvention.MaxEntityTypesSharingTable`.
Task B9: complete (commits e3111b76f..e0377c384, review clean)
Task B10: implementer DONE_WITH_CONCERNS — commit d741ad1346. Sweep: `EFCore.Relational.Tests` 1501/1500/0 failed; `EFCore.ApiBaseline.Tests` 14/14; `EFCore.Sqlite.FunctionalTests` 38420 total / 177 failed, all confirmed `mod_spatialite` environmental with 0 other causes; `EFCore.Design.Tests` 1275 total / 5 failed, confirmed pre-existing in untouched `OperationExecutorTest.cs`. **`EFCore.SqlServer.FunctionalTests` not run — no instance available. That coverage is unverified and must run before any PR opens.**
Task B10: I checked the baseline growth myself rather than accept it — 100 `"Member"` entries added to `EFCore.Relational.baseline.json` (+6 in Design, from B8's `CSharpSnapshotGenerator` additions). My first cut suggested 63 were unrelated to the workstream, but that was my grep being wrong: member signatures do not repeat their declaring type name, so entries like `bool IsNameOverridden { get; }`, `StoreObjectIdentifier DependentStoreObject { get; init; }` and `override int GetHashCode();` are the override interfaces' and `StoreObjectPair`'s own members. All 100 are legitimately from this workstream; the baseline is clean and the PR diff is not polluted.
Task B10: complete (commit e0377c384..d741ad134). No separate task review — folded into the final whole-branch review, since B10 is baseline regeneration plus a sweep and its own evidence is the sweep itself.

## Final whole-branch review

Dispatched (opus, most capable per Model Selection) against 2965e616bf..d741ad1346 — 14 commits, ~443KB. Handed it the five deferred/parked items to triage, the spec §4 checklist to verify coverage against, the stale-`build.sh` warning, and the LSP guidance. Asked specifically for the cross-cutting checks per-task reviews structurally could not do: key/FK family consistency, overrides silently dropped between subsystems, `StoreObjectPair` equality everywhere it is relied on, and anything orphaned by a later task's change of approach.

Final review: **Needs work before PR** — 0 Critical, 2 Important, 10 Minor, plus triage of all five deferred items and a full spec §4 checklist.

Ruling: **F19 — my deferred-item-1 ruling was wrong, and the final review caught it.** I recorded the missing `OwnershipBuilder` store-object overloads as cosmetic API asymmetry to note in the PR body. It is not cosmetic: `GenerateForeignKeyOverrides` is called inside `if (!foreignKey.IsOwnership)` (`CSharpSnapshotGenerator.cs:1839`, call at `:1864`), and `RelationalAnnotationNames.ForeignKeyOverrides` is in `IgnoredRelationalAnnotations` (`AnnotationCodeGenerator.cs:39`), so **neither path emits an override for an ownership FK** — configure a per-table constraint name on an owned type in its own table, run `migrations add`, and it silently vanishes. I verified both halves myself. What I missed is why upstream added those overloads at all: the *global* `HasConstraintName` reaches ownership FKs as a `MethodCallCodeFragment` and is emitted regardless of ownership, so the new hand-emitted family inherits an ownership gate that `.OnDelete`/`.IsRequired` need and constraint names do not. Fixing. Cost of my original ruling had it stood: a lossy design-time round trip shipped to upstream with no error.

Ruling: **F20 — the new validation is over-broad and must be scoped to the feature.** `ValidateSharedForeignKeyNameOverrides` (`RelationalModelValidator.cs:1930-1938`) throws whenever two structurally identical, `AreCompatible` FKs on one table resolve to different names — nothing in that condition involves an override. Two entity types sharing a table with their own explicit *global* `HasConstraintName` hit it, and pass today because `ValidateSharedForeignKeysCompatibility` buckets by resolved name and never compares them. That is a behavioural break bundled into a PR scoped to #27972/#27971 — exactly what the EF team asks to be split out. The method name, the resource string `DuplicateConstraintNameOverride`, and the spec checklist all promise a feature-scoped check. Gating on at least one name coming from an override restores the method to its own name. Cost if wrong: a narrower check that misses a conflict the broader one would have caught — recoverable, and the right default for an upstream PR.

Final review — deferred-item triage accepted as given: (1) fix before PR, per F19. (2) note in PR body. (3) leave — pre-existing upstream, and the code comment already answers the obvious question. (4) leave — keeping the diff on-subject was right. (5) **moot** — the switch to annotation-changed conventions removed the dependency on `DelayConventions()` replay entirely, so the completeness of my attach-audit no longer bears on correctness. That dissolves F12 rather than answering it.

Final review: fix wave DONE — commits 0c33058bec (Fixes 1, 3, 4, 5) and 7d3a0a1a49 (Fix 2). `EFCore.Relational.Tests` 1502/1501 (moved +1), `EFCore.Design.Tests` 1277/1271 with the 5 pre-existing failures unchanged (moved +2), `EFCore.ApiBaseline.Tests` 14/14 with the baseline showing exactly the two new `OwnershipBuilder` members. **The ownership-FK bug reproduced before the fix — a wrong default name was emitted — and is correct after.** That is the empirical proof F19 was a real defect rather than a code-reading inference; the final reviewer had flagged it as its one unverified conclusion.
Final review: scoped re-review dispatched against d741ad1346..7d3a0a1a49. Asked specifically whether Fix 2 narrows rather than guts the validation (must still throw on genuine conflicting overrides, and must consider an override on **either** side of the comparison), and whether moving the overrides call out of the `!IsOwnership` block disturbed the `.OnDelete`/`.IsRequired`/`.IsConstrained` guards that legitimately belong there.

Final review: (earlier) ONE fix wave dispatched — both Importants plus three cheap Minors (deterministic `GetValues()` ordering, FK explicit-null snapshot symmetry, a comment justifying a `!` in `MergeInto`). Remaining Minors go to the PR body. Per the skill there is no second wave; residuals get adjudicated.

Task B10: (earlier) implementer stalled waiting on a backgrounded Sqlite run — **third occurrence** of this failure mode (B4, B6, B10), despite the no-backgrounding rule being in the initial dispatch this time. Work is present but uncommitted: both `EFCore.Relational.baseline.json` and — unanticipated — `EFCore.Design.baseline.json`, plus the two F7 doc fixes. Confirmed a Sqlite test process is genuinely still alive, so the run is real. Resumed with an instruction to poll to completion rather than end its turn.

Process finding for any future run of this skill: telling an implementer "do not background a test run" in the dispatch is **not sufficient** — three agents did it anyway. The instruction needs to be paired with an explicit polling recipe (check process, sleep, check again) rather than a prohibition, because the agent backgrounds the run precisely when it expects the wait to exceed its patience.

Task B10: (original dispatch) — BASE e0377c3846. Carries the F3 correction (baseline.json is read by `test/EFCore.ApiBaseline.Tests`, not `RelationalApiConsistencyTest`), the F7 doc-wart cleanup, and a hard stop before the push/PR step.

Task B9: (original dispatch) — BASE e3111b76ff. **The highest-risk task in the phase**: its end-to-end test is the empirical proof of ruling F8, that a finalizing convention can attach an override to the entity-splitting linking FK. If that fails, the workstream does not deliver its driving use case and I need to know immediately rather than after an hour of work.

Task B7+B8: (original dispatch) — BASE 20f4e42f60. Batched because both tasks are the same problem (make the overrides survive a round-trip) over adjacent code, so one review seat covers them.

Task B6: (earlier) scoped re-review dispatched against 4e66f8c570..20f4e42f60. Also asked it to reconcile the test-count arithmetic — the Sqlite total moved 38226→38227, a net +1, while the implementer describes "both new tests".
Task B6: (earlier) implementer ended its turn waiting on a backgrounded Sqlite run — second time this failure mode has cost a task (B4 was the first). Eight files modified, nothing committed. Resumed with the state, the warm-node loop, and an explicit "do not background a test run". **Process note for the remaining tasks: put the no-backgrounding instruction and the fast loop in the *initial* dispatch, not the resume.**
Task B6: (original dispatch) — BASE 1333fae5c9. Carried F11 (Sqlite is the runner), F5, F6/F8, the F9 fast loop, and a warning that `RelationalApiConsistencyTest`'s `FluentApiTypes` already covers both builder-extension classes B6 modifies.

Task B5: (original dispatch) — BASE 73dc2c7381. Handed it the four attach chains I already verified delayed (`InternalEntityTypeBuilder.cs:1693`→`:1397`, `InternalModelBuilder.cs:263`→`:118`, `InternalEntityTypeBuilder.cs:132`→`:100`, `InternalTypeBaseBuilder.cs:1421`→`:1393`) so it finishes the list rather than redoing it, plus F2, F4, F5, and the F8 warning to keep its FK test on an ordinary navigated relationship rather than a linking FK.
Task B4: minor (deferred): the brief's single end-to-end test became three. The dropped linking-FK assertion is replaced by `Foreign_key_name_override_reaches_the_relational_model` over a `Blog`/`Post` fixture, which exercises the same `RelationalModel` → `GetConstraintName` path on an FK whose identity is stable across finalization. Carried F1 (register the FK quartet in `RelationalApiConsistencyTest`), F2 (no extension methods), F4 (test-snippet substitutions), the F5 lesson (a `FinalizeModel()` test using `SplitToTable` needs a non-key column left on the principal table), B1's commit as the pattern to mirror, and the new report-format rule (paste the raw runner summary line).

Ruling: **F4 — the plan's test snippets do not compile in `test/EFCore.Relational.Tests`.** Two substitutions apply to every Phase-B test written into that project: `RelationalTestHelpers.Instance` → `FakeRelationalTestHelpers.Instance`, and `[ConditionalFact]` → `[Fact]`. Why: verified `RelationalTestHelpers` (`test/EFCore.Relational.Specification.Tests/TestUtilities/RelationalTestHelpers.cs:6`) is **abstract** and in a different project, while `FakeRelationalTestHelpers` (`test/EFCore.Relational.Tests/TestUtilities/FakeRelationalTestHelpers.cs:8`) is the concrete local one; and zero files in `EFCore.Relational.Tests` use `ConditionalFact` (25 repo-wide, none here). The substitution is **project-scoped** — B6's test lands in `EFCore.Relational.Specification.Tests`, which uses the fixture's own `CreateModelBuilder()` and must not be rewritten this way. Carry into the B2, B3, B5 and B9 dispatches. Cost if wrong: compile errors caught on the task's first build.

